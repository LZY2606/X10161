[package]
name = "reassembly-bench"
version = "0.1.0"
edition = "2021"
description = "离线抓包网络会话重组台：解析链路层/IPv4/IPv6/TCP，归并双向流为会话代次并重组字节流"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
sha2 = "0.10"
