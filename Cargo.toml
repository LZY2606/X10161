[package]
name = "session-reassembly-workbench"
version = "0.1.0"
edition = "2021"
description = "网络会话重组台：离线抓包的 TCP 会话重组与证据分析"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
sha2 = "0.10"

[profile.release]
opt-level = 2
