[package]
name = "reasm-bench"
version = "0.1.0"
edition = "2021"
description = "网络会话重组台: offline capture session reassembly workbench"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
sha2 = "0.10"
hex = "0.4"
